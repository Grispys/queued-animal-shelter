package org.example;

public class Main {
    public static void main(String[] args) {
        QueueArray shelter = new QueueArray(10);
        shelter.isEmpty();
        shelter.enQueue(1);
        shelter.enQueue(0);
        shelter.enQueue(1);
        shelter.enQueue(0);
        shelter.enQueue(1);
        shelter.enQueue(0);
        shelter.enQueue(1);
        shelter.enQueue(0);
        shelter.enQueue(1);
        shelter.enQueue(0);
        shelter.getQueue();


    }
}